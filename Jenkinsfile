pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'echo "Hello from Jenkins"'
            }
        }
        stage('2nd stage') {
            steps {
                sh 'ls -la'
            }
        }
    }
}
